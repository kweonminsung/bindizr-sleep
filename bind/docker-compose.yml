version: "3.8"

services:
  sleep:
    build:
      context: .
      dockerfile: Dockerfile
    # ports:
    #   - "53:53/udp"
    #   - "53:53/tcp"
    #   - "953:953/tcp"
    restart: unless-stopped
    volumes:
      - bind-data:/etc/bind

volumes:
  bind-data:
